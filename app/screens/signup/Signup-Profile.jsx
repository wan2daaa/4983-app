import SignupProfileLayout from '@components/signup/signup-profile/signup-profile-layout/SignupProfileLayout';

const SignupProfile = ({navigation}) => {
  return <SignupProfileLayout navigation={navigation} />;
};

export default SignupProfile;
